{
  "name": "Citi-mazon",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "NPM_CONFIG_LOGLEVEL=error uv run pywrangler dev",
    "deploy": "NPM_CONFIG_LOGLEVEL=error uv run pywrangler deploy",
    "start": "NPM_CONFIG_LOGLEVEL=error uv run pywrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}