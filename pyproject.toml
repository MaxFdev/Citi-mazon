[project]
name = "Citi-mazon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["flask", "supabase"]

[dependency-groups]
dev = ["workers-py", "workers-runtime-sdk"]
